
const Add = (x, y) => {
    return x + y;
}

const Sub = (x, y) => {
    return x - y;
}

const Mul = (x, y) => {
    return x * y;
}

const Div = (x, y) => {
    return x / y;
}

export { Add, Sub, Mul, Div};